import { PlaceholderPage } from '@/components/PlaceholderPage';
export const metadata = { title: 'Gallery' };
export default function GalleryPage() {
  return (
    <PlaceholderPage
      phase="Phase 3"
      title="Gallery"
      description="A filterable gallery of finished sticks, restorations, competition entries, and workshop life — coming in Phase 3."
    />
  );
}
