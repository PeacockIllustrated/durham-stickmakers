import { PlaceholderPage } from '@/components/PlaceholderPage';
export const metadata = { title: 'Delivery' };
export default function DeliveryPage() {
  return <PlaceholderPage phase="Phase 4" title="Delivery & returns" description="Shipping info for oversized parcels and our returns policy - coming in Phase 4." />;
}
