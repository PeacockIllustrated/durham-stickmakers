import { AdminStubPage } from '@/components/AdminStubPage';
export const metadata = { title: 'Messages' };
export default function AdminMessagesPage() {
  return <AdminStubPage title="Messages" phase="Phase 3 (with contact form)" />;
}
