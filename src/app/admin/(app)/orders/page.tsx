import { AdminStubPage } from '@/components/AdminStubPage';
export const metadata = { title: 'Orders' };
export default function AdminOrdersPage() {
  return <AdminStubPage title="Orders" phase="Phase 2 (with Stripe webhooks)" />;
}
